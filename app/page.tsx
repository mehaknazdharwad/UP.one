'use client';
import {useEffect,useState} from 'react';
import {ArrowDownToLine,ArrowRight,Bell,CheckCircle2,ChevronRight,Clock3,FileText,LayoutDashboard,MapPin,Search,ShieldCheck,TriangleAlert,Users,X,Zap,Plus,Menu} from 'lucide-react';
import {categories,districts,officers,statuses,transitions,done,due,remaining,type Complaint} from '../lib/domain';
const dictionary:Record<string,string>={'Overview':'अवलोकन','Complaints':'शिकायतें','SLA & escalations':'समय सीमा एवं एस्केलेशन','Officers':'अधिकारी','District insights':'ज़िला विश्लेषण','Electrical Department':'विद्युत विभाग','Government of Uttar Pradesh':'उत्तर प्रदेश सरकार','Department overview':'विभाग का अवलोकन','Monitor grievances. Prioritize action. Keep UP connected.':'शिकायतों की निगरानी करें। समय पर कार्रवाई करें।','All districts':'सभी ज़िले','Last 30 days':'पिछले 30 दिन','All time':'संपूर्ण अवधि','Export report':'रिपोर्ट डाउनलोड','New complaint':'नई शिकायत','Total complaints':'कुल शिकायतें','Active complaints':'सक्रिय शिकायतें','Resolved & closed':'निस्तारित एवं बंद','SLA compliance':'समय सीमा अनुपालन','Within the 7-day target':'7 दिन की सीमा के भीतर','Across selected districts':'चयनित ज़िलों में','Awaiting resolution':'निस्तारण की प्रतीक्षा','Successfully addressed':'निस्तारण पूर्ण','Needs your attention':'आपके ध्यान की आवश्यकता','overdue complaints':'समय सीमा पार शिकायतें','critical cases':'गंभीर मामले','approaching SLA':'समय सीमा के निकट','Review cases':'मामले देखें','Complaint trends':'शिकायतों का रुझान','Reported and resolved over time':'समय के साथ दर्ज एवं निस्तारित','Reported':'दर्ज','Resolved':'निस्तारित','Complaint breakdown':'शिकायतों का विवरण','By issue category':'समस्या के प्रकार अनुसार','Priority queue':'प्राथमिकता सूची','Cases that need timely intervention':'समय पर कार्रवाई की आवश्यकता वाले मामले','View all complaints':'सभी शिकायतें देखें','Complaint':'शिकायत','District':'ज़िला','Priority':'प्राथमिकता','Status':'स्थिति','Assigned officer':'नियुक्त अधिकारी','SLA status':'समय सीमा','Search complaints…':'शिकायतें खोजें…','All statuses':'सभी स्थितियाँ','All priorities':'सभी प्राथमिकताएँ','New':'नई','Assigned':'नियुक्त','In progress':'कार्य प्रगति पर','Closed':'बंद','Reopened':'पुनः खोली गई','Escalated':'एस्केलेटेड','Critical':'गंभीर','High':'उच्च','Normal':'सामान्य','Power outage':'बिजली कटौती','Transformer':'ट्रांसफॉर्मर','Damaged wire / pole':'क्षतिग्रस्त तार / खंभा','Streetlight':'स्ट्रीट लाइट','Voltage fluctuation':'वोल्टेज उतार-चढ़ाव','Other electrical':'अन्य विद्युत समस्या','Unassigned':'नियुक्ति शेष','Overdue':'विलंबित','days left':'दिन शेष','days overdue':'दिन विलंबित','Officer workload':'अधिकारी कार्यभार','Active assignments and overdue cases':'सक्रिय नियुक्तियाँ एवं विलंबित मामले','View officers':'अधिकारी देखें','active':'सक्रिय','overdue':'विलंबित','District concentration':'ज़िलावार शिकायतें','Complaints by district':'ज़िलावार शिकायतों की संख्या','Recurring issues':'बार-बार आने वाली समस्याएँ','Repeated categories within a district':'एक ज़िले में दोहराए गए समस्या प्रकार','No complaints found':'कोई शिकायत नहीं मिली','Try changing your filters.':'अपने फ़िल्टर बदलकर देखें।','Loading complaints…':'शिकायतें लोड हो रही हैं…','Retry':'पुनः प्रयास','Demonstration workspace':'प्रदर्शन कार्यक्षेत्र','Sample records · not connected to government systems':'नमूना रिकॉर्ड · सरकारी प्रणाली से जुड़ा नहीं','Load sample complaints':'नमूना शिकायतें लोड करें','Start with sample cases or register a new complaint.':'नमूना मामले लोड करें या नई शिकायत दर्ज करें।','Department Head':'विभागाध्यक्ष','Operations':'संचालन','Uttar Pradesh':'उत्तर प्रदेश','7-day resolution target':'7 दिन में निस्तारण लक्ष्य','Complaint details':'शिकायत का विवरण','Location':'स्थान','Citizen':'नागरिक','Reported on':'दर्ज करने की तिथि','Due by':'अंतिम तिथि','Resolution details':'निस्तारण विवरण','Not resolved yet':'निस्तारण शेष','Activity & history':'गतिविधि एवं इतिहास','Update complaint':'शिकायत अपडेट करें','Activity note / resolution details':'गतिविधि नोट / निस्तारण विवरण','Describe the action taken…':'की गई कार्रवाई का विवरण…','Save update':'अपडेट सहेजें','Saving…':'सहेजा जा रहा है…','Title':'शीर्षक','Issue category':'समस्या का प्रकार','Full address / landmark':'पूरा पता / स्थलचिह्न','Citizen name':'नागरिक का नाम','Register complaint':'शिकायत दर्ज करें','Cancel':'रद्द करें','Close':'बंद करें','Complaint saved successfully':'शिकायत सफलतापूर्वक सहेजी गई','Showing':'दिखाई जा रही हैं','of':'में से','Previous':'पिछला','Next':'अगला','All cases':'सभी मामले','Due within 48 hours':'48 घंटे के भीतर देय','SLA clock starts when reported; reopening keeps the original deadline.':'समय सीमा दर्ज होने से शुरू होती है; दोबारा खोलने पर मूल समय सीमा लागू रहती है।','On time':'समय पर','Late':'विलंबित','No completed cases':'कोई पूर्ण मामला नहीं','Clear filters':'फ़िल्टर हटाएँ','Lucknow':'लखनऊ','Kanpur Nagar':'कानपुर नगर','Varanasi':'वाराणसी','Prayagraj':'प्रयागराज','Agra':'आगरा','Gorakhpur':'गोरखपुर','Meerut':'मेरठ','Bareilly':'बरेली'};
export default function Dashboard(){
 const [lang,setLang]=useState('en'),[view,setView]=useState('Overview'),[items,setItems]=useState<Complaint[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[district,setDistrict]=useState(''),[period,setPeriod]=useState('30'),[query,setQuery]=useState(''),[status,setStatus]=useState(''),[priority,setPriority]=useState(''),[risk,setRisk]=useState(''),[officerFilter,setOfficerFilter]=useState(''),[selected,setSelected]=useState<Complaint|null>(null),[creating,setCreating]=useState(false),[saving,setSaving]=useState(false),[toast,setToast]=useState(''),[page,setPage]=useState(0),[mobile,setMobile]=useState(false);
 const t=(s:string)=>lang==='hi'?(dictionary[s]||s):s; const date=(s:string)=>new Date(s).toLocaleDateString(lang==='hi'?'hi-IN':'en-IN',{day:'numeric',month:'short',year:'numeric'});
 async function load(){setLoading(true);setError('');try{const r=await fetch('/api/complaints');const d=await r.json() as Complaint & {error:string;complaints:Complaint[]};if(!r.ok)throw Error(d.error);setItems(d.complaints);}catch(e){setError((e as Error).message);}finally{setLoading(false);}}
 useEffect(()=>{load();setLang(localStorage.getItem('up-language')||'en');},[]);useEffect(()=>{document.documentElement.lang=lang;localStorage.setItem('up-language',lang);},[lang]);useEffect(()=>{setPage(0);},[query,status,priority,district,risk,view,period,officerFilter]);useEffect(()=>{if(toast){const id=setTimeout(()=>setToast(''),5000);return()=>clearTimeout(id);}},[toast]);
 const base=items.filter(c=>(!district||c.district===district)&&(period==='all'||Date.now()-new Date(c.createdAt).getTime()<=30*86400000));const active=base.filter(c=>!done(c)),complete=base.filter(done),overdue=active.filter(c=>remaining(c)<0),critical=active.filter(c=>c.priority==='Critical'),near=active.filter(c=>remaining(c)>=0&&remaining(c)<=2);const compliance=complete.length?Math.round(complete.filter(c=>c.resolvedAt&&+new Date(c.resolvedAt)<=due(c)).length/complete.length*100):null;
 const filtered=base.filter(c=>(!status||c.status===status)&&(!priority||c.priority===priority)&&(!officerFilter||c.officer===officerFilter)&&(!query||[c.id,c.category,c.title,c.citizen,c.address,c.district,t(c.category),t(c.district)].join(' ').toLowerCase().includes(query.toLowerCase()))&&(!risk||(!done(c)&&(risk==='overdue'?remaining(c)<0:risk==='critical'?c.priority==='Critical':remaining(c)>=0&&remaining(c)<=2)))).sort((a,b)=>Number(done(a))-Number(done(b))||due(a)-due(b));
 function navigate(v:string,r=''){setView(v);setRisk(r);setStatus('');setPriority('');setOfficerFilter('');setQuery('');setMobile(false);}
 async function save(body:Record<string,unknown>,method='PATCH'){setSaving(true);try{const r=await fetch('/api/complaints',{method,headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json() as Complaint & {error:string;complaints:Complaint[]};if(!r.ok)throw Error(d.error);await load();if(method==='PATCH')setSelected(d);else setCreating(false);setToast(t('Complaint saved successfully'));return true;}catch(e){setToast((e as Error).message);return false;}finally{setSaving(false);}}
 function exportCSV(){const rows=[['ID','Title','Category','District','Priority','Status','Officer','Reported','Due','Resolution'],...filtered.map(c=>[c.id,c.title,c.category,c.district,c.priority,c.status,c.officer,c.createdAt,new Date(due(c)).toISOString(),c.resolution])];const url=URL.createObjectURL(new Blob(['\ufeff'+rows.map(row=>row.map(v=>'"'+(/^[=+@-]/.test(v)?"'":'')+v.replaceAll('"','""')+'"').join(',')).join('\r\n')],{type:'text/csv;charset=utf-8;'}));const a=document.createElement('a');a.href=url;a.download='up-electrical-complaints.csv';a.click();URL.revokeObjectURL(url);}
 const badge=(s:string)=><span className={'badge '+s.toLowerCase().replaceAll(' ','-')}>{t(s)}</span>;
 const sla=(c:Complaint)=>done(c)?<span className={c.resolvedAt&&+new Date(c.resolvedAt)>due(c)?'late':'good'}>{t(c.resolvedAt&&+new Date(c.resolvedAt)>due(c)?'Late':'On time')}</span>:<span className={remaining(c)<0?'late':remaining(c)<=2?'warning':'muted'}>{Math.ceil(Math.abs(remaining(c)))} {t(remaining(c)<0?'days overdue':'days left')}</span>;
 const table=(data:Complaint[])=><div className="table-scroll"><table><thead><tr>{['Complaint','District','Priority','Status','Assigned officer','SLA status'].map(s=><th key={s}>{t(s)}</th>)}<th/></tr></thead><tbody>{data.map(c=><tr key={c.id}><td><button className="case-link" onClick={()=>setSelected(c)}><small>{c.id}</small><strong>{t(c.category)}</strong></button></td><td>{t(c.district)}</td><td>{badge(c.priority)}</td><td>{badge(c.status)}</td><td><span className="person">{c.officer&&<i>{c.officer.split(' ').map(x=>x[0]).join('')}</i>}{c.officer||t('Unassigned')}</span></td><td>{sla(c)}</td><td><button className="icon-button" aria-label={`${t('Complaint details')} ${c.id}`} onClick={()=>setSelected(c)}><ChevronRight size={17}/></button></td></tr>)}</tbody></table>{!data.length&&<div className="empty"><Search/><h3>{t('No complaints found')}</h3><p>{t('Try changing your filters.')}</p></div>}</div>;
 return <div className="shell"><aside className={'sidebar '+(mobile?'expanded':'')}><div className="brand"><span className="brand-icon"><Zap size={25} fill="currentColor"/></span><div>जन समाधान<small>JAN SAMADHAN</small></div></div><div className="department"><span>{t('Government of Uttar Pradesh')}</span><strong>{t('Electrical Department')}</strong></div><div className="nav-caption">{t('Operations')}</div><nav>{[{name:'Overview',icon:LayoutDashboard},{name:'Complaints',icon:FileText},{name:'SLA & escalations',icon:Clock3},{name:'Officers',icon:Users},{name:'District insights',icon:MapPin}].map(n=><button className={view===n.name?'active':''} onClick={()=>navigate(n.name)} key={n.name}><n.icon size={19}/>{t(n.name)}{n.name==='Complaints'&&<b>{items.length}</b>}</button>)}</nav><div className="sidebar-bottom"><div className="target"><ShieldCheck size={23}/><div>{t('7-day resolution target')}<small>{t('Uttar Pradesh')}</small></div></div><div className="profile"><span>DH</span><div>{t('Department Head')}<small>{t('Electrical Department')}</small></div></div></div></aside><div className="workspace"><header className="topbar"><div className="breadcrumb"><button className="mobile-toggle icon-button" aria-label="Menu" onClick={()=>setMobile(!mobile)}><Menu/></button>{t('Electrical Department')}<ChevronRight size={14}/><b>{t(view)}</b></div><div className="top-actions"><button className="language" onClick={()=>setLang(lang==='en'?'hi':'en')}><span className={lang==='en'?'chosen':''}>EN</span><span className={lang==='hi'?'chosen':''}>हिंदी</span></button><button className="notification icon-button" aria-label={t('Needs your attention')} onClick={()=>navigate('SLA & escalations','critical')}><Bell size={20}/>{critical.length>0&&<i/>}</button><span className="avatar">DH</span></div></header><main><div className="page-heading"><div><div className="eyebrow">{t('Uttar Pradesh')} <span>/</span> {t('Operations')}</div><h1>{t(view==='Overview'?'Department overview':view)}</h1><p>{t('Monitor grievances. Prioritize action. Keep UP connected.')}</p></div><button className="button" onClick={exportCSV}><ArrowDownToLine size={16}/>{t('Export report')}</button></div><div className="toolbar"><div className="filters"><label><MapPin size={16}/><select aria-label={t('District')} value={district} onChange={e=>setDistrict(e.target.value)}><option value="">{t('All districts')}</option>{districts.map(d=><option key={d} value={d}>{t(d)}</option>)}</select></label><label><Clock3 size={16}/><select aria-label={t('Last 30 days')} value={period} onChange={e=>setPeriod(e.target.value)}><option value="30">{t('Last 30 days')}</option><option value="all">{t('All time')}</option></select></label></div><button className="button primary" onClick={()=>setCreating(true)}><Plus size={17}/>{t('New complaint')}</button></div><div className="demo-label">{t('Demonstration workspace')} <span>· {t('Sample records · not connected to government systems')}</span></div>
 {error?<div className="error">{error}<button onClick={load}>{t('Retry')}</button></div>:loading?<div className="empty">{t('Loading complaints…')}</div>:items.length===0?<section className="panel empty"><Zap size={32}/><h2>{t('No complaints found')}</h2><p>{t('Start with sample cases or register a new complaint.')}</p><button disabled={saving} className="button primary" onClick={()=>save({action:'seed'},'POST')}>{t('Load sample complaints')}</button></section>:<>
 {view==='Overview'&&<><div className="stats">{[{label:'Total complaints',value:base.length,icon:FileText,sub:'Across selected districts',cls:'blue'},{label:'Active complaints',value:active.length,icon:Clock3,sub:'Awaiting resolution',cls:'orange'},{label:'Resolved & closed',value:complete.length,icon:CheckCircle2,sub:'Successfully addressed',cls:'green'},{label:'SLA compliance',value:compliance===null?'—':`${compliance}%`,icon:ShieldCheck,sub:compliance===null?'No completed cases':'Within the 7-day target',cls:'purple'}].map(s=><section className="stat panel" key={s.label}><div><span>{t(s.label)}</span><i className={s.cls}><s.icon size={19}/></i></div><strong>{s.value}</strong><small>{t(s.sub)}</small></section>)}</div><section className="attention"><TriangleAlert size={23}/><div><strong>{t('Needs your attention')}</strong><p><b>{overdue.length}</b> {t('overdue complaints')}<span>·</span><b>{critical.length}</b> {t('critical cases')}<span>·</span><b>{near.length}</b> {t('approaching SLA')}</p></div><button onClick={()=>navigate('SLA & escalations','overdue')}>{t('Review cases')}<ArrowRight size={16}/></button></section><div className="charts"><section className="panel"><div className="panel-heading"><div><h2>{t('Complaint trends')}</h2><p>{t('Reported and resolved over time')}</p></div><div className="legend"><span><i/>{t('Reported')}</span><span><i className="green-dot"/>{t('Resolved')}</span></div></div><Trend items={base} t={t} lang={lang}/></section><section className="panel breakdown"><div className="panel-heading"><div><h2>{t('Complaint breakdown')}</h2><p>{t('By issue category')}</p></div></div>{categories.map((cat,i)=>{const n=base.filter(c=>c.category===cat).length;const color=['#3974d5','#e9a23c','#7956bc','#36a694','#da6376','#9aa8bd'][i];return <button className="category" key={cat} onClick={()=>{navigate('Complaints');setQuery(cat);}}><span><i style={{background:color}}/>{t(cat)}<b>{n}</b></span><div className="track"><i style={{width:`${base.length?n/base.length*100:0}%`,background:color}}/></div></button>})}</section></div><section className="panel queue"><div className="panel-heading"><div><h2>{t('Priority queue')}<span className="count">{active.length}</span></h2><p>{t('Cases that need timely intervention')}</p></div><button className="text-button" onClick={()=>navigate('Complaints')}>{t('View all complaints')}<ArrowRight size={15}/></button></div>{table([...active].sort((a,b)=>Number(b.priority==='Critical')-Number(a.priority==='Critical')||due(a)-due(b)).slice(0,5))}</section></>}
 {(view==='Complaints'||view==='SLA & escalations')&&<>{view==='SLA & escalations'&&<><div className="risk-tabs">{[['overdue','Overdue',overdue.length],['near','Due within 48 hours',near.length],['critical','Critical',critical.length],['','All cases',base.length]].map(([r,label,n])=><button className={'panel '+(risk===r?'selected':'')} onClick={()=>setRisk(String(r))} key={r}><span>{t(String(label))}</span><strong>{n}</strong></button>)}</div><p className="sla-note">{t('SLA clock starts when reported; reopening keeps the original deadline.')}</p></>}<section className="panel"><div className="list-filters"><label className="search"><Search size={17}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder={t('Search complaints…')}/></label><select aria-label={t('Status')} value={status} onChange={e=>setStatus(e.target.value)}><option value="">{t('All statuses')}</option>{statuses.map(s=><option key={s} value={s}>{t(s)}</option>)}</select><select aria-label={t('Priority')} value={priority} onChange={e=>setPriority(e.target.value)}><option value="">{t('All priorities')}</option>{['Critical','High','Normal'].map(s=><option key={s} value={s}>{t(s)}</option>)}</select><button className="icon-button" title={t('Clear filters')} onClick={()=>{setQuery('');setStatus('');setPriority('');setRisk('');setOfficerFilter('');}}><X size={17}/></button></div>{officerFilter&&<p className="sla-note">{t('Assigned officer')}: {officerFilter}</p>}{table(filtered.slice(page*12,page*12+12))}<div className="pagination"><span>{t('Showing')} {filtered.length?page*12+1:0}–{Math.min(page*12+12,filtered.length)} {t('of')} {filtered.length}</span><div><button className="button" disabled={!page} onClick={()=>setPage(page-1)}>{t('Previous')}</button><button className="button" disabled={(page+1)*12>=filtered.length} onClick={()=>setPage(page+1)}>{t('Next')}</button></div></div></section></>}
 {(view==='Overview'||view==='Officers'||view==='District insights')&&<div className="bottom-grid">{view!=='District insights'&&<section className="panel"><div className="panel-heading"><div><h2>{t('Officer workload')}</h2><p>{t('Active assignments and overdue cases')}</p></div>{view==='Overview'&&<button className="text-button" onClick={()=>navigate('Officers')}>{t('View officers')}<ArrowRight size={15}/></button>}</div>{officers.map((o,i)=>{const assigned=active.filter(c=>c.officer===o),late=assigned.filter(c=>remaining(c)<0).length;return <button className="officer-row" key={o} onClick={()=>{navigate('Complaints');setOfficerFilter(o);}}><span className={'officer-avatar color-'+i}>{o.split(' ').map(s=>s[0]).join('')}</span><div><strong>{o}</strong><small>{assigned.length} {t('active')} · <span className={late?'late':''}>{late} {t('overdue')}</span></small></div><div className="workload-bar"><i style={{width:`${Math.min(assigned.length/15*100,100)}%`}}/></div><ChevronRight size={15}/></button>})}</section>}{view!=='Officers'&&<section className="panel"><div className="panel-heading"><div><h2>{t('District concentration')}</h2><p>{t('Complaints by district')}</p></div><MapPin size={18}/></div>{districts.map(d=>({d,n:base.filter(c=>c.district===d).length})).sort((a,b)=>b.n-a.n).map(({d,n},i)=><button className="district-row" key={d} onClick={()=>{navigate('Complaints');setDistrict(d);}}><span className="rank">{String(i+1).padStart(2,'0')}</span><strong>{t(d)}</strong><div className="district-bar"><i style={{width:`${n/Math.max(1,...districts.map(x=>base.filter(c=>c.district===x).length))*100}%`}}/></div><b>{n}</b><ChevronRight size={15}/></button>)}</section>}{view==='District insights'&&<section className="panel"><div className="panel-heading"><div><h2>{t('Recurring issues')}</h2><p>{t('Repeated categories within a district')}</p></div></div>{districts.flatMap(d=>categories.map(c=>({d,c,n:base.filter(x=>x.district===d&&x.category===c).length}))).filter(x=>x.n>1).sort((a,b)=>b.n-a.n).slice(0,10).map(x=><button className="recurring" key={x.d+x.c} onClick={()=>{navigate('Complaints');setDistrict(x.d);setQuery(x.c);}}><div><strong>{t(x.c)}</strong><small>{t(x.d)}</small></div><b>{x.n}</b><ChevronRight size={16}/></button>)}</section>}</div>}
 </>}<footer><span>{t('Government of Uttar Pradesh')} · {t('Electrical Department')}</span><span>{t('Demonstration workspace')}</span></footer></main></div>{toast&&<div className="toast" role="status">{toast}</div>}{selected&&<ComplaintDialog key={selected.id+'-'+selected.version} c={selected} t={t} date={date} sla={sla} saving={saving} close={()=>setSelected(null)} save={save}/>}{creating&&<NewDialog t={t} saving={saving} close={()=>setCreating(false)} save={save}/>}</div>;
}
function Trend({items,t,lang}:{items:Complaint[];t:(s:string)=>string;lang:string}){const bins=Array.from({length:6},(_,i)=>{const start=Date.now()-(30-i*5)*86400000,end=start+5*86400000;return {label:new Date(start).toLocaleDateString(lang==='hi'?'hi-IN':'en-IN',{day:'numeric',month:'short'}),reported:items.filter(c=>+new Date(c.createdAt)>=start&&+new Date(c.createdAt)<end).length,resolved:items.filter(c=>c.resolvedAt&&+new Date(c.resolvedAt)>=start&&+new Date(c.resolvedAt)<end).length};});const max=Math.max(5,...bins.flatMap(b=>[b.reported,b.resolved]));const points=(key:'reported'|'resolved')=>bins.map((b,i)=>`${48+i*102},${178-b[key]/max*140}`).join(' ');return <div className="trend-chart"><svg viewBox="0 0 600 220" role="img" aria-label={t('Complaint trends')}><defs><linearGradient id="area" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#3673d9" stopOpacity=".15"/><stop offset="100%" stopColor="#3673d9" stopOpacity="0"/></linearGradient></defs>{[0,1,2,3,4].map(i=><g key={i}><line x1="48" x2="558" y1={38+i*35} y2={38+i*35} stroke="#e9edf3" strokeDasharray="4 4"/><text x="28" y={42+i*35} textAnchor="end">{Math.round(max*(1-i/4))}</text></g>)}<polygon points={`48,178 ${points('reported')} 558,178`} fill="url(#area)"/><polyline points={points('reported')} stroke="#3775dc" strokeWidth="3" strokeLinejoin="round" fill="none"/><polyline points={points('resolved')} stroke="#36a694" strokeWidth="2.5" strokeLinejoin="round" fill="none"/>{bins.map((b,i)=><g key={i}><circle cx={48+i*102} cy={178-b.reported/max*140} r="4" fill="#fff" stroke="#3775dc" strokeWidth="2"><title>{b.label}: {t('Reported')} {b.reported}, {t('Resolved')} {b.resolved}</title></circle><text x={48+i*102} y="208" textAnchor="middle">{b.label}</text></g>)}</svg></div>}
function Dialog({children,close,title}:{children:React.ReactNode;close:()=>void;title:string}){useEffect(()=>{const before=document.activeElement as HTMLElement;const key=(e:KeyboardEvent)=>{if(e.key==='Escape')close();if(e.key==='Tab'){const els=Array.from(document.querySelectorAll<HTMLElement>('.dialog button:not(:disabled),.dialog input,.dialog select,.dialog textarea'));const first=els[0],last=els[els.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last?.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first?.focus();}}};document.addEventListener('keydown',key);document.body.style.overflow='hidden';(document.querySelector('.dialog button') as HTMLElement)?.focus();return()=>{document.removeEventListener('keydown',key);document.body.style.overflow='';before?.focus();};},[]);return <div className="overlay" onMouseDown={e=>{if(e.target===e.currentTarget)close();}}><section className="dialog" role="dialog" aria-modal="true" aria-label={title}><header><h2>{title}</h2><button className="icon-button" aria-label="Close / बंद करें" onClick={close}><X/></button></header>{children}</section></div>}
function ComplaintDialog({c,t,date,sla,saving,close,save}:{c:Complaint;t:(s:string)=>string;date:(s:string)=>string;sla:(c:Complaint)=>React.ReactNode;saving:boolean;close:()=>void;save:(b:Record<string,unknown>,m?:string)=>Promise<boolean>}){const [status,setStatus]=useState(c.status),[officer,setOfficer]=useState(c.officer),[note,setNote]=useState('');return <Dialog close={close} title={t('Complaint details')}><div className="dialog-body"><span className="eyebrow">{c.id}</span><h2>{t(c.category)}</h2><p>{c.title}</p><div className="detail-fields">{[['Status',t(c.status)],['Priority',t(c.priority)],['Location',`${t(c.district)} · ${c.address}`],['Citizen',c.citizen],['Assigned officer',c.officer||t('Unassigned')],['Reported on',date(c.createdAt)],['Due by',date(new Date(due(c)).toISOString())]].map(([a,b])=><div key={a}><small>{t(a)}</small><strong>{b}</strong></div>)}<div><small>{t('SLA status')}</small>{sla(c)}</div></div><p className="sla-note">{t('SLA clock starts when reported; reopening keeps the original deadline.')}</p><h3>{t('Resolution details')}</h3><p>{c.resolution||t('Not resolved yet')}</p><form onSubmit={async e=>{e.preventDefault();if(await save({id:c.id,version:c.version,status,officer,note}))setNote('');}}><h3>{t('Update complaint')}</h3><div className="form-grid"><label>{t('Assigned officer')}<select value={officer} onChange={e=>setOfficer(e.target.value)} required={status!=='New'}><option value="">{t('Unassigned')}</option>{officers.map(o=><option key={o}>{o}</option>)}</select></label><label>{t('Status')}<select value={status} onChange={e=>setStatus(e.target.value)}>{[c.status,...transitions[c.status]].map(s=><option key={s} value={s}>{t(s)}</option>)}</select></label></div><label>{t('Activity note / resolution details')}<textarea required maxLength={2000} value={note} onChange={e=>setNote(e.target.value)} placeholder={t('Describe the action taken…')}/></label><button className="button primary" disabled={saving}>{t(saving?'Saving…':'Save update')}</button></form><h3>{t('Activity & history')}</h3><div className="timeline">{[...c.history].reverse().map((h,i)=><div key={i}><i/><strong>{t(h.action)}</strong><small>{date(h.at)} · {new Date(h.at).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}</small><p>{h.note}</p></div>)}</div></div></Dialog>}
function NewDialog({t,saving,close,save}:{t:(s:string)=>string;saving:boolean;close:()=>void;save:(b:Record<string,unknown>,m?:string)=>Promise<boolean>}){return <Dialog close={close} title={t('New complaint')}><form className="dialog-body" onSubmit={e=>{e.preventDefault();save(Object.fromEntries(new FormData(e.currentTarget)),'POST');}}><label>{t('Title')}<input name="title" required maxLength={500}/></label><div className="form-grid"><label>{t('Issue category')}<select name="category">{categories.map(c=><option key={c} value={c}>{t(c)}</option>)}</select></label><label>{t('District')}<select name="district">{districts.map(c=><option key={c} value={c}>{t(c)}</option>)}</select></label></div><label>{t('Full address / landmark')}<textarea name="address" required maxLength={500}/></label><div className="form-grid"><label>{t('Citizen name')}<input name="citizen" required maxLength={500}/></label><label>{t('Priority')}<select name="priority">{['Normal','High','Critical'].map(c=><option key={c} value={c}>{t(c)}</option>)}</select></label></div><div className="form-actions"><button type="button" className="button" onClick={close}>{t('Cancel')}</button><button className="button primary" disabled={saving}>{t(saving?'Saving…':'Register complaint')}</button></div></form></Dialog>}

